node_modules/
dist/
.next/
.astro/
.turbo/
coverage/
pnpm-lock.yaml
*.tsbuildinfo

# TOML has no Prettier parser without a plugin (fly.toml, etc.)
*.toml

# Astro has no Prettier parser without prettier-plugin-astro; the one .astro
# page is hand-formatted (packages/site-builder).
*.astro

# Generated by `wrangler types` — never hand-edited.
worker-configuration.d.ts
.wrangler/

# Prose docs are hand-authored — don't let Prettier reflow tables/prose.
*.md
