# Resolve missing peer deps automatically, but still fail on genuine peer conflicts.
auto-install-peers=true
strict-peer-dependencies=true

# Enforce the Node/pnpm floor from package.json `engines` at install time.
engine-strict=true
