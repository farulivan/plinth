/**
 * Business logic for the custom-domains domain — verifies ownership and maps a
 * hostname to a workspace. Framework-agnostic (no Hono) per ADR-0009. Impl lands
 * with custom-domain wiring (ADR-0004).
 */
export {};
