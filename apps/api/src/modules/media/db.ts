/**
 * Data access for the media domain — workspace-scoped Drizzle queries against the
 * media table. The only media layer that talks to Postgres (ADR-0009); callers
 * reach it through service. Impl lands with the media pipeline.
 */
export {};
