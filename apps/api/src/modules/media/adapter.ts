/**
 * External-SDK boundary for the media domain — Cloudflare R2 object I/O and Sharp
 * transforms (ADR-0006). Pure vendor calls; never imports service or db
 * (ADR-0009). Impl lands with the media pipeline.
 */
export {};
